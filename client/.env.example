VITE_SERVER_URL=http://localhost:3001

# Optional: enables "Sign in with Google" via Clerk alongside the nickname
# flow. Leave unset to run anon-only. Get it from your Clerk dashboard's
# API Keys page.
VITE_CLERK_PUBLISHABLE_KEY=
